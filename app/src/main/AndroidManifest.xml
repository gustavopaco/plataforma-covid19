<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.cotemig.covid19">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Covid19"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activities.HistoricoPaisActivity"></activity>
        <activity android:name=".ui.activities.HomeActivity" />
        <activity android:name=".ui.activities.SplashActivty">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.SummaryActivity" />
        <activity android:name=".ui.activities.CasosEstadosActivity" />
        <activity android:name=".ui.activities.NoticiasActivity" />
        <activity android:name=".ui.activities.SintomasActivity" />
    </application>

</manifest>